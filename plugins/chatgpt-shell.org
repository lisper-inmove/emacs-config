#+begin_src emacs-lisp
  (use-package chatgpt-shell
    :ensure t)
  (setq chatgpt-shell-deepseek-key (getenv "CHATGPT_SHELL_DS_Emacs01"))

  (global-set-key (kbd "C-c C-g") 'chatgpt-shell-prompt-compose)
  (global-set-key (kbd "C-c C-t") 'chatgpt-shell-prompt-compose-send-buffer)
#+end_src
